public interface Comparable<Student> {
//    int compareTo(Student StudentToCompare);
//    int compareFragmentTo(Student StudentToCompare);

    int compareTo(String lastName);

    int compareFragmentTo(String lastName);
}
